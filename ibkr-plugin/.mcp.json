{
  "mcpServers": {
    "ibkr": {
      "type": "http",
      "url": "${user_config.ibkr_url}",
      "title": "IBKR market data",
      "description": "Historical bars, event windows and coverage from a local read-only Interactive Brokers store. It cannot place an order.",
      "headers": {
        "Authorization": "Bearer ${user_config.ibkr_token}"
      }
    }
  }
}
